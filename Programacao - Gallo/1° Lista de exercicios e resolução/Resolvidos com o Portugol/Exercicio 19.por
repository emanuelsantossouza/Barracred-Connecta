programa {
  // Calcular a área de um círculo dado o raio.
  funcao inicio() {
    real raioCirculo
    real pi = 3.14

    escreva("Qual é o raio do circulo: ")
    leia(raioCirculo)

    escreva("A área do círculo é: ", (raioCirculo * raioCirculo) * pi)
  }
}
